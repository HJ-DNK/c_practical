#include <stdio.h>
void main()
{
    int marks;
    printf("enter the marks \n");               // 
    scanf("%d", &marks);                        // user input marks 
    marks > 22 ? printf("pass\n") : printf("fail\n");  // it  conditon op op?op:op
}
